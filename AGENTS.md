# AGENTS.md

**日本語で返答してください**

## プロジェクト概要

Gradioでドット絵化をするPythonプログラムです。

仕様
- GradioでWebUIを表示
- 入力された画像をドット絵化(Pixel Art化)
- 処理内容
    - 縮小→拡大（モザイク化）
    - ピクセルの色をk色にkmeansで分類しそれぞれの平均値を求め、色を置き換え減色します
- オプション処理
    - ガウシアンフィルタ
    - エロージョン
    - 減色(kmeans)
    - 彩度調節(なし/弱/強)
    - 色温度調節(-35 ~ +35)

## 主要技術スタック・コーディング規約

- 言語: Python 3.12
- フレームワーク: 

**プロジェクトルール**

- python 3.12のモダンな機能を活用する
- 型ヒントを積極的に使用する
- ruff準拠のコードスタイルを守る
- 適切な単位でモジュール化・クラス化を行う
- ロジック部分はクラス化・関数化してテスト可能にする

## 開発環境とコマンド

### 基本コマンド（直接実行）
> **メモ**: uv の制限を避けるため、必ず `. .venv/bin/activate` で仮想環境を有効化してからコマンドを実行してください。

```bash
# 仮想環境を有効化
. .venv/bin/activate

# アプリ実行
python main.py
```

### リンターとフォーマット（直接実行）
```bash
# Ruff実行
. .venv/bin/activate
ruff check
ruff check <file_name> --fix
```
